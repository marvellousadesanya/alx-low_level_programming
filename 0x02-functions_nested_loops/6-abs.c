#include "main.h"

/**
 * _abs - Entry point
 *
 * Description: 'Compute absolute value'
 *
 * @h: Checks for h
 *
 * Return: Always 0 (SUCCESS)
 */

int _abs(int h)
{
	if (h <= 0 && h >=0)
	{
		return (0);
	}
}
