#include "main.h"

/*
 * _islower(int c) - Checks for lowercase
 *
 * Return 0 or 1
 */

int islower(int c);

int _islower(int c)
{
	char c;

	if (islower(char c))
	{
		return (0);
	}
	else
	{
		return (1);
	}
}

