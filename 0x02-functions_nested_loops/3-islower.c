#include "main.h"

/*
 * _islower(int c) - Checks for lowercase
 *
 * Return 0 or 1
 */

int islower(int c);

int _islower(int c)
{
	int c;

	if (islower(int c))
	{
		return (0);
	}
	else
	{
		return (1);
	}
}

